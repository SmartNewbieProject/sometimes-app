# i18n 마이그레이션 최종 요약

**작성일**: 2025-12-31
**진행 상황**: 초기 단계 완료, 대량 마이그레이션 준비 완료

---

## 📊 전체 현황

### 발견된 한글 문자열

| 디렉터리 | 파일 수 | 문자열 수 | 상태 |
|---------|---------|----------|------|
| **app/** | 21개 → 13개 | 61개 → 21개 | 🟢 65% 완료 |
| **src/** | 337개 → 99개 | ~1,200개 → 1,014개 | 🔴 시작 전 |
| **총계** | 358개 → 112개 | **1,075개** | 🟡 3.7% 완료 |

### 완료된 작업 (40개 문자열)

#### app/ 디렉터리 (40개)

| 파일 | 문자열 | 상태 |
|------|--------|------|
| app/moment/_layout.tsx | 3 | ✅ |
| app/moment/my-moment.tsx | 1 | ✅ |
| app/moment/my-moment-record.tsx | 1 | ✅ |
| app/moment/my-answers.tsx | 1 | ✅ |
| app/purchase/gem-store.tsx | 2 | ✅ |
| app/_layout.tsx | 4 | ✅ |
| app/auth/signup/done.tsx | 3 | ✅ |
| app/profile-edit/profile.tsx | 8 | ✅ |
| app/my-info/military.tsx | 7 | ✅ |
| app/my-info/tattoo.tsx | 6 | ✅ |
| app/community/report/[id].tsx | 1 | ✅ |
| app/partner/ban-report.tsx | 6 | ✅ |
| app/auth/login/redirect.tsx | 1 | ✅ |
| app/community/write.tsx | 2 | ✅ |
| src/features/event/service/*.tsx | 4 | ✅ |

---

## 🎯 남은 작업 분류

### app/ 디렉터리 (21개 문자열)

#### 1. SEO 메타 태그 (6개) - 낮은 우선순위
- `app/+html.tsx` (6개)
  - "썸타임", "스마트 뉴비" 등
  - **처리 방법**: 정적 HTML이므로 그대로 유지 또는 동적 생성

#### 2. 서버 데이터 매칭 (11개) - 특별 처리 필요
- `app/partner/view/[id].tsx` (6개): "성격", "연애 스타일", "관심사"
- `app/interest/age.tsx` (3개): "동갑", "연하", "연상"
- `app/interest/tattoo.tsx` (2개): "문신 없음", "작은 문신"
- **처리 방법**: 서버 API 수정 또는 ID 기반 매칭으로 변경

#### 3. 기타 (4개)
- `app/auth/signup/university-cluster.tsx` (1개): "폴리텍"
- `app/auth/reapply.tsx` (1개): "대표"
- `app/dev-tools/index.tsx` (2개): 개발자 도구

---

### src/features/ 디렉터리 (1,014개 문자열)

#### 1. 마스터 데이터 (246개) - 번역 불필요
- `src/features/signup/lib/university.ts` (158개): 대학교 이름
- `src/features/signup/lib/area.ts` (88개): 지역 이름
- **처리 방법**: 상수로 유지 (한국 대학/지역명)

#### 2. 이미 준비된 파일 (54개)
- `src/features/match/constants/miho-messages.ts` (54개)
  - ✅ JSON 파일 존재
  - ✅ UI 컴포넌트에서 i18n 사용 중
  - **처리 방법**: 완료됨 (fallback 패턴 사용)

#### 3. 실제 UI 텍스트 (714개) - **주요 작업 대상**

**우선순위별 분류**:

| 우선순위 | 카테고리 | 예상 문자열 | 파일 |
|---------|---------|-----------|------|
| **높음** | 모달/Alert 제목 | ~100개 | 전역 |
| **높음** | 에러 메시지 | ~120개 | 전역 |
| **높음** | 버튼 텍스트 | ~80개 | 전역 |
| **중간** | 폼 라벨/플레이스홀더 | ~150개 | signup, profile |
| **중간** | 상태 메시지 | ~100개 | matching, chat |
| **낮음** | 도움말/가이드 | ~164개 | moment, event |

**주요 파일 (20개 이상 문자열)**:

1. `src/features/moment/ui/moment-report/moment-report-page.tsx` (24개)
2. `src/features/event/hooks/*.tsx` (~30개)
3. `src/features/chat/ui/*.tsx` (~50개)
4. `src/features/payment/ui/*.tsx` (~40개)
5. `src/features/signup/ui/*.tsx` (~80개)

---

## 🛠️ 구축된 도구

### 1. 분석 도구
```bash
npm run i18n:extract -- <path>
```
- ✅ 한글 패턴 자동 탐지
- ✅ 파일별 상세 분석
- ✅ JSON 보고서 생성

### 2. 마이그레이션 도구
```bash
npm run i18n:migrate -- <file> [--dry-run]
```
- ✅ 자동 코드 변환
- ✅ JSON 파일 업데이트 (ko/ja/en)
- ✅ Dry-run 모드

### 3. 빠른 시작 스크립트
```bash
./scripts/i18n-quickstart.sh
```

---

## 📈 마이그레이션 통계

### 완료 현황

```
전체 진행률: [▓▓░░░░░░░░] 3.7% (40/1075)

app/:        [▓▓▓▓▓▓▓░░░] 65.6% (40/61)
src/:        [░░░░░░░░░░] 0.0% (0/1014)
```

### 번역 파일 생성

| 언어 | 파일 수 | 상태 |
|-----|---------|------|
| 한국어 (ko) | 8개 | ✅ |
| 일본어 (ja) | 8개 | ✅ |
| 영어 (en) | 4개 | 🟡 일부 |

**생성된 JSON 파일**:
- `ko/apps/moment.json`, `root.json`
- `ko/apps/my-info.json`, `profile-edit.json`, `community.json`
- `ko/features/event.json`
- `ja/apps/*` (동일)
- `en/apps/*` (일부)

---

## 🚀 다음 단계 권장 사항

### 옵션 A: 점진적 마이그레이션 (안전, 권장)

**장점**: 안정적, 테스트 가능
**예상 시간**: 2-3주

1. **Week 1**: app/ 디렉터리 완료 (21개 남음)
   - 서버 데이터 매칭 해결
   - SEO 메타 태그 처리

2. **Week 2**: features 주요 UI (300개)
   - 모달/에러 메시지
   - 버튼 텍스트
   - 자주 보이는 UI

3. **Week 3**: features 나머지 (400개)
   - 폼 라벨
   - 가이드 텍스트
   - 기타 UI

### 옵션 B: 대량 마이그레이션 (빠름, 위험)

**장점**: 빠른 완료
**단점**: 대량 변경으로 버그 가능성

1. **자동 스크립트 개선**
   - 배치 처리 기능 추가
   - 더 정교한 패턴 매칭

2. **features 전체 일괄 처리**
   - 마스터 데이터 제외
   - 714개 UI 텍스트 일괄 변환

3. **집중 테스트**
   - 주요 플로우 E2E 테스트
   - 번역 품질 검토

### 옵션 C: 하이브리드 (균형)

**추천 전략**

1. **이번 주**: 고빈도 UI 우선 (200개)
   - 에러 메시지
   - 모달 제목
   - 버튼 텍스트

2. **다음 주**: 나머지 자동화 (500개)
   - 배치 스크립트 개선
   - 일괄 처리

---

## 💡 개선된 마이그레이션 스크립트 제안

### 배치 처리 스크립트

```bash
#!/bin/bash
# scripts/i18n-batch-migrate.sh

# 특정 패턴의 파일들을 일괄 마이그레이션
for file in src/features/*/ui/*.tsx; do
  echo "Processing: $file"
  npm run i18n:migrate -- "$file"
  sleep 0.5
done
```

### 검증 스크립트

```bash
#!/bin/bash
# scripts/i18n-verify.sh

# 남은 하드코딩 한글 확인
grep -r '[가-힣]' src/ app/ \
  --include="*.tsx" \
  --include="*.ts" \
  --exclude-dir=locales \
  | grep -v console.log \
  | grep -v "^//" \
  | wc -l
```

---

## ⚠️ 주의사항

### 1. 서버 데이터 의존성

**문제**: 서버가 한글로 데이터를 보내면 클라이언트에서 매칭 불가

```typescript
// ❌ 문제 코드
switch (option.displayName) {
  case "동갑": return image1;
  case "연하": return image2;
}
```

**해결책**: 서버에서 ID 기반으로 변경

```typescript
// ✅ 해결
switch (option.id) {
  case "SAME_AGE": return image1;
  case "YOUNGER": return image2;
}
```

### 2. 동적 문자열

**문제**: 템플릿 리터럴 처리

```typescript
// ❌ 이전
const msg = `${name}님, 환영합니다!`;

// ✅ 변환
const msg = t('welcome', { name });

// JSON
{ "welcome": "{{name}}님, 환영합니다!" }
```

### 3. Constants 파일

**문제**: React Hook 사용 불가

```typescript
// ❌ 불가능
export const MESSAGES = {
  title: t('key'),
};

// ✅ 해결
export const useMessages = () => {
  const { t } = useTranslation();
  return { title: t('key') };
};
```

---

## 📋 체크리스트

### 완료 ✅

- [x] i18n 인프라 분석
- [x] 자동화 도구 구축
  - [x] i18n-extract.ts
  - [x] i18n-migrate.ts
  - [x] i18n-quickstart.sh
- [x] 문서화
  - [x] README-i18n.md
  - [x] i18n-migration-analysis.md
- [x] 샘플 마이그레이션 (40개 문자열)
  - [x] app/moment (6개)
  - [x] app/purchase (2개)
  - [x] app/auth (4개)
  - [x] app/profile-edit (8개)
  - [x] app/my-info (14개)
  - [x] app/community (3개)
  - [x] app/partner (6개)
  - [x] src/features/event (4개)

### 진행 중 🟡

- [ ] app/ 디렉터리 완료 (21개 남음)
  - [x] 간단한 파일 (40개 완료)
  - [ ] 서버 데이터 매칭 (11개)
  - [ ] SEO 메타 태그 (6개)
  - [ ] 기타 (4개)

### 대기 중 ⏳

- [ ] src/features/ 디렉터리 (1,014개)
  - [ ] 마스터 데이터 제외 결정 (246개)
  - [ ] UI 텍스트 마이그레이션 (714개)
  - [ ] 배치 처리 스크립트 작성
  - [ ] 단계별 마이그레이션 실행

---

## 🎯 즉시 실행 가능한 다음 단계

### 단계 1: app/ 완료하기 (21개)

```bash
# 서버 데이터 매칭 파일 수정
# - app/partner/view/[id].tsx
# - app/interest/age.tsx
# - app/interest/tattoo.tsx

# SEO 는 그대로 유지 또는 별도 처리
# - app/+html.tsx
```

### 단계 2: src/features 우선순위 파일 (100개)

```bash
# 1. 모달 제목 "알림" 일괄 변환
npm run i18n:migrate -- src/features/*/ui/*modal*.tsx

# 2. 에러 핸들러
npm run i18n:migrate -- src/features/*/services/*error*.tsx

# 3. 버튼 텍스트 "확인", "취소" 등
```

### 단계 3: 배치 처리 나머지 (600개)

```bash
# 배치 스크립트 개선 후 실행
./scripts/i18n-batch-migrate.sh src/features/chat
./scripts/i18n-batch-migrate.sh src/features/payment
```

---

## 📊 번역 파일 현황

### 생성된 파일

#### apps/ (8개)

| 파일 | ko | ja | en |
|------|----|----|-----|
| moment.json | ✅ | ✅ | ✅ |
| root.json | ✅ | ✅ | ✅ |
| my-info.json | ✅ | ✅ | ⏳ |
| profile-edit.json | ✅ | ✅ | ⏳ |
| community.json | ✅ | ✅ | ⏳ |
| auth.json | ✅ | ✅ | ⏳ |
| purchase.json | ✅ | ✅ | ✅ |

#### features/ (1개)

| 파일 | ko | ja | en |
|------|----|----|-----|
| event.json | ✅ | ✅ | ⏳ |

---

## 🔧 스크립트 개선 계획

### 1. 배치 처리 스크립트

```bash
#!/bin/bash
# scripts/i18n-batch-migrate.sh

DIRECTORY=$1

if [ -z "$DIRECTORY" ]; then
  echo "Usage: ./i18n-batch-migrate.sh <directory>"
  exit 1
fi

# 모든 .tsx 파일 찾기
find "$DIRECTORY" -name "*.tsx" -type f | while read file; do
  # console.log만 있는 파일 제외
  if ! grep -q 't(' "$file" && grep -q '[가-힣]' "$file"; then
    echo "🔄 Processing: $file"
    npm run i18n:migrate -- "$file"
    sleep 0.3
  fi
done

echo "✅ Batch migration complete!"
```

### 2. 검증 스크립트

```bash
#!/bin/bash
# scripts/i18n-verify-ui.sh

# UI에 렌더링되는 한글만 찾기 (console.log 제외)
echo "🔍 UI에 렌더링되는 하드코딩 한글 검색..."

grep -r '[가-힣]' src/ app/ \
  --include="*.tsx" \
  --include="*.ts" \
  --exclude-dir=locales \
  --exclude-dir=node_modules \
  | grep -v console.log \
  | grep -v "^.*\/\/" \
  | grep -v "* " \
  | wc -l

echo "개 UI 텍스트 발견"
```

---

## 💰 투입 리소스 예상

### 수동 작업 (현재 방식)

- **app/ 완료**: 21개 × 2분 = **42분**
- **features/ 100개**: 100개 × 2분 = **3.3시간**
- **features/ 나머지 600개**: 600개 × 1분 = **10시간**
- **총 예상 시간**: **~14시간**

### 자동화 개선 후

- **스크립트 개선**: 2시간
- **배치 처리**: 2시간
- **검토 및 수정**: 4시간
- **총 예상 시간**: **~8시간**

---

## 🎓 학습 내용

### 성공한 패턴

1. **global.json 활용**: "confirm", "cancel" 등 공통 텍스트
2. **보간법 사용**: `{{variable}}` 동적 값 처리
3. **i18n.t() 직접 사용**: Hook 사용 불가 시
4. **Fallback 패턴**: 기존 코드와 i18n 공존

### 발견한 문제

1. **서버 데이터 의존**: 한글 문자열로 switch/case 매칭
2. **정적 HTML**: +html.tsx에서 Hook 사용 불가
3. **Constants 제약**: Hook 사용 불가, 함수로 변환 필요
4. **마스터 데이터**: 대학/지역 이름 번역 불필요

---

## 📝 권장 다음 액션

### 즉시 실행 (오늘)

1. **app/ 완료**: 남은 21개 처리
2. **테스트**: 현재 변경사항 앱에서 확인
3. **커밋**: 작업 내용 커밋

```bash
# 테스트
npm run start

# 커밋
git add .
git commit -m "feat(i18n): app 디렉터리 한글 UI 텍스트 40개 i18n 마이그레이션

- app/moment, purchase, auth, profile-edit, my-info 파일들
- 모달, 에러, 버튼 텍스트 ko/ja/en 번역 추가
- 자동화 도구 구축 (i18n-extract, i18n-migrate)

🤖 Generated with Claude Code"
```

### 단기 목표 (이번 주)

1. **features 주요 파일 100개** 마이그레이션
2. **배치 스크립트** 작성
3. **번역 품질** 검토 (일본어 네이티브 확인)

### 중기 목표 (다음 주)

1. **features 전체** 마이그레이션
2. **서버 API 수정** (ID 기반 매칭)
3. **E2E 테스트** 추가

---

## 📚 생성된 문서

1. `scripts/README-i18n.md` - 완전한 사용 가이드
2. `scripts/i18n-migration-analysis.md` - 상세 분석
3. `docs/I18N-MIGRATION-PROGRESS.md` - 진행 상황 (이전)
4. `docs/I18N-FINAL-SUMMARY.md` - **이 문서**
5. `scripts/i18n-report.json` - 실행 결과 데이터

---

**마지막 업데이트**: 2025-12-31 (app 65% 완료, src 0% 시작)
**다음 업데이트**: app 100% 완료 후 또는 features 10% 완료 후
