# Global Matching - Phase 1 Requirements

## Original Requirement

> "글로벌 매칭(한일 크로스보더) 기능의 프론트엔드 전체 구현. API 문서 기반으로 온보딩, 후보 탐색, 좋아요/편지, 받은 좋아요, 채팅 번역, 상태/설정까지 전반적인 화면 UI/UX와 API 연동 작업."

---

## Design Principle: 매칭 프로세스 통일

> **핵심 원칙**: 글로벌 매칭은 기존 국내 매칭과 **동일한 프로세스 · 동일한 UI**를 사용한다.
> 별도 화면이 아닌 **매칭 모드 전환**으로 구현하여 앱의 일관된 경험을 유지한다.
> 기존 컴포넌트에 **최소한의 분기 렌더링**만 추가한다.

---

## Clarified Specification

### Goal

글로벌 매칭 Phase 1 — 홈 세그먼트 컨트롤 기반 모드 전환 + 기존 매칭 플로우 재사용 + 채팅 번역 + 설정/상태

### Scope

| 포함 | 제외 |
|------|------|
| 홈 상단 세그먼트 컨트롤 [국내] / [글로벌] | Phase 2+ 기능 (배지, 기념일 등) |
| 3단계 온보딩 슬라이드 (글로벌 탭 첫 클릭 시) | 푸시 알림 / 딥링크 |
| 글로벌 매칭 1명씩 표시 (IdleMatchTimer 재사용) | A/B 테스트 인프라 |
| 좋아요/편지 전송 (partner/view 재사용) | 백엔드 API 구현 |
| 받은 좋아요 post-box 통합 (국기 배지 구분) | 카드 스와이프 UI (폐기) |
| 매칭 성립 축하 모달 | 별도 글로벌 좋아요 모달 (폐기) |
| 채팅 이중 버블 (원문+번역) | |
| 프로필 내 글로벌 매칭 설정/상태 | |

### UX Strategy (Revised)

| 섹션 | 원래 계획 | 확정안 | 변경 사유 |
|------|----------|--------|----------|
| 진입점 | A. 홈 전용 카드 | **세그먼트 컨트롤** | 매칭 모드 통일 원칙 |
| 후보 탐색 | A. 카드 스와이프 | **IdleMatchTimer 재사용 (1명씩)** | domestic 동일 경험 |
| 좋아요 & 편지 | 별도 모달 | **partner/view + LikeButton 재사용** | domestic 동일 경험 |
| 받은 좋아요 | 홈 내 별도 섹션 | **post-box 통합 (혼합)** | UI 통합 |
| 채팅 번역 | A. 이중 버블 | **A. 이중 버블 (유지)** | - |
| 상태 & 설정 | 프로필 내 섹션 | **프로필 내 섹션 (유지)** | - |

### Target Users

KR + JP 양쪽 시점 UI 모두 구현

| 관점 | KR 사용자 | JP 사용자 |
|------|----------|----------|
| 후보 탐색 대상 | JP 프로필 (한국어 번역) | KR 프로필 (일본어 번역) |
| 좋아요 비용 | 16구슬 | 무료 |
| 번역 방향 | 일본어 → 한국어 | 한국어 → 일본어 |
| 프로필 표시 | 번역만 표시 (nameTranslated 등) | 번역만 표시 |

---

## Screen Flow (Revised)

```
홈 화면
  ├─ 세그먼트 컨트롤: [국내 매칭] / [글로벌]
  │
  ├─ [국내] 선택 → 기존 domestic 매칭 (변경 없음)
  │
  └─ [글로벌] 선택
       ├─ 온보딩 미완료 → 온보딩 페이지 자동 이동 (3단계)
       │     Step 1: 소개 (핵심 가치 + 16구슬 비용)
       │     Step 2: 언어 실력 (4단계 Radio)
       │     Step 3: 관심 이유 (Chip 복수 선택)
       │     → POST /api/global-matching/onboarding
       │     → 홈으로 복귀 (글로벌 탭 활성)
       │
       └─ 온보딩 완료 → IdleMatchTimer (글로벌 모드)
             │
             ├─ type='open' → 글로벌 파트너 카드 표시
             │     (번역된 프로필, 국기 배지, 48h 타이머)
             │     └─ "더보기" → partner/view/[id] (글로벌 모드)
             │           ├─ 번역된 프로필 정보 표시
             │           ├─ LikeButton (구슬 비용 분기: KR 16 / JP 무료)
             │           ├─ 편지 기능 (100자, 선택적)
             │           └─ 좋아요 전송 → POST /api/global-matching/like
             │
             ├─ type='waiting' → 대기 화면 (다음 매칭까지 카운트다운)
             ├─ type='not-found' → 글로벌 후보 없음
             └─ type='rematching' → 리매칭 중

받은 좋아요 (post-box 통합):
  ├─ 기존 받은 좋아요 목록에 글로벌 좋아요 혼합 표시
  ├─ 국기 배지 (🇯🇵 / 🇰🇷)로 글로벌 구분
  ├─ 편지 내용 (번역) 표시
  ├─ 48h 타이머 표시
  ├─ 수락 → POST /api/global-matching/accept → 축하 모달 → 글로벌 채팅방
  └─ 거절 → DELETE /api/global-matching/decline

글로벌 채팅방:
  ├─ 이중 버블 (원문 + 구분선 + 번역)
  ├─ KR/JP 양쪽 시점 자동 적용
  └─ is_global_chat 플래그로 기존 채팅과 구분

프로필 > 설정:
  ├─ 글로벌 매칭 활성화 상태 표시
  ├─ 통계 (보낸 좋아요, 매칭 수 등)
  └─ 설정 바텀시트 (ON/OFF, 언어 실력 변경, 관심 이유 수정)
```

---

## Implementation Architecture

### 기존 컴포넌트 재사용 전략

| 기존 컴포넌트 | 수정 사항 | 분기 조건 |
|-------------|----------|----------|
| `app/home/index.tsx` | 세그먼트 컨트롤 추가 | - |
| `IdleMatchTimer` | 매칭 모드에 따라 API 분기 | `matchingMode: 'domestic' \| 'global'` |
| `Partner` (idle-match-timer/ui) | 국기 배지 + 번역 프로필 표시 | `isGlobalMatch` 플래그 |
| `partner/view/[id]` | 번역 프로필 표시 + 글로벌 좋아요 API | `isGlobalMatch` 쿼리 파라미터 |
| `LikeButton` | 구슬 비용 분기 (KR 16 / JP 0) | `isGlobalMatch` prop |
| `LikeOptionModal` | 비용 텍스트 분기 | `isGlobalMatch` prop |
| `LikeCollapse` | 글로벌 좋아요 포함 | 국기 배지 렌더링 |
| `PostBoxCard` | 국기 배지 + 번역 편지 표시 | `isGlobalMatch` 필드 |
| 채팅 메시지 버블 | 이중 버블 렌더링 | `is_global_chat` 플래그 |

### 신규 컴포넌트

| 컴포넌트 | 위치 | 설명 |
|---------|------|------|
| `MatchingModeSegment` | `src/features/global-matching/ui/` | 홈 상단 세그먼트 컨트롤 |
| 온보딩 페이지 | `app/global-matching/onboarding/` | 3단계 슬라이드 |
| `GlobalMatchingSettings` | `src/features/global-matching/ui/` | 프로필 내 설정 섹션 |
| `DualBubble` | `src/features/global-matching/ui/` | 채팅 이중 버블 |
| `CountryBadge` | `src/shared/ui/` or `src/features/global-matching/ui/` | 국기 배지 |

### 상태 관리

```typescript
// 매칭 모드 상태 (Zustand or local state)
matchingMode: 'domestic' | 'global'

// React Query Keys (글로벌 매칭)
["global-matching", "status"]           // 글로벌 매칭 상태
["global-matching", "latest"]           // 현재 글로벌 매칭 (domestic과 동일 형태)
["global-matching", "received-likes"]   // 받은 글로벌 좋아요

// 기존 쿼리 키 (확장)
["liked", "to-me"]  // 받은 좋아요 (글로벌 포함)
["liked", "of-me"]  // 보낸 좋아요 (글로벌 포함)
```

### API Layer

```typescript
// src/features/global-matching/apis/index.ts
completeOnboarding(preferenceOptionIds?: string[]): Promise<GlobalMatchingStatus>
getStatus(): Promise<GlobalMatchingStatus>
getLatestGlobalMatching(): Promise<MatchDetails>  // ← 백엔드에 요청 예정 (domestic 동일 형태)
sendGlobalLike(targetUserId: string, letterContent?: string): Promise<LikeResult>
getReceivedGlobalLikes(): Promise<ReceivedLike[]>
acceptGlobalLike(likeId: string): Promise<AcceptResult>
declineGlobalLike(likeId: string): Promise<void>
```

### 백엔드 요청 사항

> **중요**: 글로벌 매칭도 domestic과 동일한 형태의 매칭 API 필요
>
> 기존 `GET /v2/matching` 형태로 글로벌 전용 엔드포인트 요청:
> - 서버가 1명씩 배정
> - `endOfView` 타이머 포함
> - `type: 'open' | 'waiting' | 'not-found' | 'rematching'` 상태
> - 번역된 프로필 정보 포함
>
> 현재 API 스펙의 `GET /api/global-matching/candidates` (다수 후보 반환)는
> 이 요구사항과 맞지 않으므로 백엔드팀과 협의 필요.

---

## Technical Constraints

- **Architecture**: FSD (`src/features/global-matching/`)
- **X-Country Header**: 기존 axiosClient 인터셉터 활용 (디바이스 언어 기반 자동 결정)
- **Component Reuse**: 기존 매칭 컴포넌트 최대 재사용, 최소 분기 렌더링
- **Routing**: Expo Router (`app/` 디렉터리)
- **Profile Display**: 번역된 텍스트만 표시 (원문 미표시)

---

## API Endpoints (Phase 1)

| Method | Endpoint | 용도 | 비고 |
|--------|----------|------|------|
| POST | `/api/global-matching/onboarding` | 온보딩 완료 | 확정 |
| GET | `/api/global-matching/status` | 상태 조회 | 확정 |
| GET | TBD (백엔드 협의) | 글로벌 매칭 조회 (1명, 타이머 포함) | domestic GET /v2/matching 동일 형태 필요 |
| POST | `/api/global-matching/like` | 좋아요 전송 | 확정 |
| GET | `/api/global-matching/received-likes` | 받은 좋아요 조회 | 확정 |
| POST | `/api/global-matching/accept` | 좋아요 수락 | 확정 |
| DELETE | `/api/global-matching/decline` | 좋아요 거절 | 확정 |

---

## Key Business Rules

- 좋아요 유효 기간: **48시간** (만료 시 EXPIRED 처리)
- 매칭 열람 타이머: **48시간** (domestic 동일, 백엔드에서 endOfView 제공)
- 구슬 비용: KR **16구슬** / JP **무료**
- 중복 좋아요 불가 (409 Conflict)
- 테스트 ↔ 일반 계정 혼합 매칭 불가 (403 Forbidden)
- 수락 시 양쪽 스키마에 채팅방 동시 생성 (`is_global_chat = true`)
- 채팅 메시지 자동 번역 (`content` + `content_translated`)
- 프로필 정보: **번역만 표시** (원문 미노출)
- 받은 좋아요: **post-box에 통합** (글로벌/국내 혼합, 국기 배지로 구분)

---

## Decisions Log

| # | Question | Decision | Date |
|---|----------|----------|------|
| 1 | UX 전략 (A/B/C) | 초기 A안 → **매칭 모드 통일 원칙으로 전면 수정** | 2026-02-19 |
| 2 | 구현 범위 | Phase 1 핵심 플로우 + 설정/상태 | 2026-02-19 |
| 3 | 국가 시점 | KR + JP 양쪽 모두 | 2026-02-19 |
| 4 | 채팅 번역 | Phase 1에 포함 (이중 버블) | 2026-02-19 |
| 5 | X-Country 헤더 | 기존 인터셉터 활용 (추가 작업 불필요) | 2026-02-19 |
| 6 | 진입점 | 홈 전용 카드 → **홈 상단 세그먼트 컨트롤** | 2026-02-19 |
| 7 | 후보 탐색 | 카드 스와이프 → **IdleMatchTimer 재사용 (1명씩)** | 2026-02-19 |
| 8 | 좋아요 UI | 별도 모달 → **partner/view + LikeButton 재사용** | 2026-02-19 |
| 9 | 타이머 | 없음 → **48h (백엔드에 domestic 동일 API 요청)** | 2026-02-19 |
| 10 | 받은 좋아요 | 별도 섹션 → **post-box 통합 (혼합, 국기 배지)** | 2026-02-19 |
| 11 | 번역 표시 | 원문+번역 → **번역만 표시** | 2026-02-19 |
| 12 | 온보딩 진입 | 진입 카드 → **글로벌 탭 첫 클릭 시 자동 이동** | 2026-02-19 |
