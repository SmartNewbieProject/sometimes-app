# i18n 마이그레이션 분석 보고서

## 📊 현황 요약

- **한글 문자열 발견**: 393개 파일 (src: 337개, app: 56개)
- **i18n 시스템**: i18next 기반, ko/ja/en 지원
- **구조**: FSD 아키텍처 기반 (`apps/`, `features/`, `widgets/`, `shareds/`)

## 🔍 하드코딩 패턴 분류

### 1. **UI 텍스트** (우선순위: 높음)
```typescript
// app/profile-edit/profile.tsx
emptyFields.push("음주");
emptyFields.push("흡연");
emptyFields.push("문신");
```

### 2. **모달 메시지** (우선순위: 높음)
```typescript
// src/features/match/constants/miho-messages.ts
title: '미호가 추천해요',
lines: ['음.. 뭔가 통할 것 같은', '느낌이 들어요 ✨'],
```
**상태**: JSON 파일에는 번역 존재하지만 코드에서 미사용

### 3. **에러 메시지** (우선순위: 높음)
```typescript
const errorMessage = err?.message || err?.error || "프로필 저장에 실패했습니다. 잠시 후 다시 시도해주세요.";
```

### 4. **Alert/Modal 제목** (우선순위: 높음)
```typescript
showModal({
  title: "알림",
  children: "...",
});
```

### 5. **개발/디버그 메시지** (우선순위: 낮음)
```typescript
console.log("이미지 선택이 취소되었거나 실패했습니다.");
```
**처리**: 제외 대상 (개발용)

### 6. **주석** (우선순위: 낮음)
```typescript
// Miho 메시지 상수
```
**처리**: 제외 대상

## 📋 우선순위별 작업 계획

### Phase 1: 긴급 (사용자 노출 높음)
1. ✅ 모달/Alert 메시지
2. ✅ 에러 메시지
3. ✅ UI 라벨 (버튼, 입력 필드 등)

### Phase 2: 중요 (기능적 텍스트)
1. Form validation 메시지
2. Navigation 텍스트
3. 상태 메시지 (대기 중, 로딩 등)

### Phase 3: 개선 (부가 텍스트)
1. 플레이스홀더 텍스트
2. 도움말 텍스트
3. Meta 태그 (SEO)

### 제외 대상
- ❌ console.log 메시지
- ❌ 주석
- ❌ 개발자용 로그

## 🎯 자동화 전략

### 1. 패턴 매칭 정규식
```regex
# UI 텍스트
['"`]([가-힣]+[가-힣\s!?.,()]*?)['"`]

# 모달 제목
title:\s*['"`]([가-힣]+[가-힣\s!?.,()]*?)['"`]

# Alert 메시지
Alert\.alert\s*\(\s*['"`]([가-힣]+)
```

### 2. 마이그레이션 스크립트 기능
- [x] 한글 문자열 추출
- [ ] JSON 키 자동 생성
- [ ] 코드 변환 (하드코딩 → t() 함수)
- [ ] 일본어 번역 플레이스홀더 생성

## 📂 i18n JSON 구조

```
locales/
├── ko/
│   ├── apps/           # app/ 디렉터리 번역
│   ├── features/       # src/features/ 번역
│   ├── widgets/        # src/widgets/ 번역
│   ├── shareds/        # src/shared/ 번역
│   └── global.json     # 공통 번역
├── ja/                 # 일본어 (동일 구조)
└── en/                 # 영어 (동일 구조)
```

## 📝 사례 연구: miho-messages.ts

### 현재 상태
```typescript
// src/features/match/constants/miho-messages.ts
export const COMMON_MESSAGES = [
  {
    id: 'C2',
    title: '미호가 추천해요',  // 하드코딩
    lines: ['음.. 뭔가 통할 것 같은', '느낌이 들어요 ✨'],
  }
];
```

### 목표 상태
```typescript
import { useTranslation } from 'react-i18next';

const getMihoMessage = (id: string) => {
  const { t } = useTranslation();
  return {
    title: t(`features.match.miho_messages.common.${id}.title`),
    line1: t(`features.match.miho_messages.common.${id}.line1`),
    line2: t(`features.match.miho_messages.common.${id}.line2`),
  };
};
```

### JSON 파일 (이미 존재)
```json
// src/shared/libs/locales/ko/features/match.json
{
  "miho_messages": {
    "common": {
      "C2": {
        "title": "미호가 추천해요",
        "line1": "음.. 뭔가 통할 것 같은",
        "line2": "느낌이 들어요 ✨"
      }
    }
  }
}
```

## 🚀 실행 계획

### Step 1: 파일별 한글 문자열 추출
```bash
# 정규식으로 모든 하드코딩 한글 찾기
grep -r -n '[가-힣]' src/ app/ --include="*.tsx" --include="*.ts"
```

### Step 2: 자동 마이그레이션 스크립트
```bash
npm run i18n:migrate <file-path>
```

기능:
1. 파일에서 한글 문자열 추출
2. i18n 키 자동 생성 (경로 기반)
3. JSON 파일 업데이트 (ko, ja, en)
4. 코드 변환 (하드코딩 → t() 함수)

### Step 3: 수동 검토 및 정제
- [ ] 번역 품질 확인
- [ ] 키 이름 최적화
- [ ] 일본어 번역 추가

## ⚠️ 주의사항

1. **타입 안전성**: 하드코딩 제거 시 TypeScript 타입 에러 주의
2. **동적 문자열**: 템플릿 리터럴 처리 방법 고려
3. **복수형**: 한국어/일본어는 복수형 없지만 영어는 고려 필요
4. **컨텍스트**: 같은 단어라도 맥락에 따라 다른 번역 필요

## 📈 진행 상황 추적

- [ ] Phase 1: 모달/Alert 메시지 (예상: 50개 파일)
- [ ] Phase 2: 에러 메시지 (예상: 80개 파일)
- [ ] Phase 3: UI 라벨 (예상: 150개 파일)
- [ ] Phase 4: 기타 (예상: 113개 파일)
