import apis from './apis';
import * as hooks from './hooks';
import * as queries from './queries';
import * as UI from './ui';

const Home = {
	ui: UI,
	apis,
	queries,
	hooks,
};

export default Home;
